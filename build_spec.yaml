version: 0.1
component: build
timeoutInSeconds: 10000
shell: bash
failImmediatelyOnError: true
env:
  variables:
    - encrypt_key

steps:
  - type: Command
    name: "test"
    command: |
      encrypt_key=`oci secrets secret-bundle get \
      --secret-id=ocid1.vaultsecret.oc1.iad.amaaaaaacsgkr3ya6vbuaj34566fggblxagztegvpzviqey56x32lhaxg3eq \
      --query 'data."secret-bundle-content".content' \
      --raw-output \
      | base64 -d`
      echo $encrypt_key